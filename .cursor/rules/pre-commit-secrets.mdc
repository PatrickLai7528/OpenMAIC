---
description: 每次 git commit 前必須掃描敏感資料，禁止提交真實密鑰／DB 密碼
alwaysApply: true
---

# 提交前敏感資料檢查

在本倉庫執行 **任何** `git commit`（含 amend）之前，Agent **必須先**完成敏感資料檢查；未通過則禁止提交。

## 必做步驟

1. `git status` / `git diff --cached` 確認暫存檔。
2. 掃描 staged diff，命中真實密鑰則停止：
   - `sk-api-`、`sk-ant-`、`ghp_`、`github_pat_`、`BEGIN .*PRIVATE KEY`
   - `postgres://user:password@host`（真實密碼，非 `CHANGE_ME`／`PASS`／`USER` 佔位）
3. 確認未暫存：`.env.local`、`k8s/deploy.env`、含真實密碼的 Secret YAML。
4. 文件／example 僅允許佔位符：`CHANGE_ME`、`YOUR_PG_HOST`、`sk-replace-me`、`sk-...`。

完整清單與指令見根目錄 `AGENTS.md`。

K8s 密鑰用 ENV / `k8s/apply.sh` 注入，不要寫進可 push 的 YAML。

Docker 映像由 `.github/workflows/docker-ghcr.yml` 在每次 push 建置並推到 GHCR
（`ghcr.io/<owner>/<repo>`）。部署時用 workflow 產出的 tag 作為 `IMAGE`。
