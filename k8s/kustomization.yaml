apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: ky-super-openamic
resources:
  - 00-namespace.yaml
  # Secret is created by apply.sh from env (DATABASE_URL / MINIMAX_API_KEY / …)
  - 02-configmap.yaml
  - 03-deployment.yaml
  - 04-service.yaml
  - 05-ingress.yaml
